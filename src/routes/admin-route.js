/**
 * This file manages all the routes that come in via /user/*
 */

// const express = require('express');
// const Router = express.Router();
// const bookingCtrl = require('../controllers/bookingController');
// const adminMiddleware = require('../middleware/admin-middleware');

// Router.get('/all', adminMiddleware, (req, res) => bookingCtrl.allBookings(req, res));
// Router.put('/front-desk', adminMiddleware, (req, res) => bookingCtrl.updateUser(req, res));
// Router.delete('/profit-log', adminMiddleware, (req, res) => bookingCtrl.deleteUser(req, res));

// module.exports = Router;
